# 2026-09-08 監査修正

修正ブランチ: `fix/site-audit-2026-09-08`。監査で確定した9件を修正し、削除退避先の容量不足も修正した。2026-09-08に受付・管理の両GASを新バージョンへデプロイしたとのユーザー報告を受領した。サイト側のデプロイ・実予約・実LINE送信は本セッションでは実施していない。

## 反映状況（2026-09-08）

| 対象 | 状況 | 確認の根拠・残る確認 |
|---|---|---|
| 予約受付GAS `2026.09.08-1` | 本番デプロイ済み | ユーザー報告。本番ソースとの直接照合は未実施 |
| 管理画面GAS `2026.09.08-1` | 本番デプロイ済み | ユーザー報告。本番ソースとの直接照合は未実施 |
| 受付の正式ファイル | `Code.gs`へ昇格済み | 案内した`Code.next.gs`とバイト単位で一致。旧リポジトリ基準コードは`archive/Code.2026-09-08.before-audit-fixes.gs`へ保管 |
| Sheets API・権限 | 設定状況は未確認 | 両マニフェストにSheets API v4を追加済み。本番での有効化状態は未照合 |
| 紹介制度の初期設定 | 実行状況は未確認 | `setupReferralProgram()`の実行はデプロイ完了報告に含まれていない |
| サイト側の修正 | 本セッションでは未デプロイ | 本番の反映状況は未確認 |
| 本番の動作確認 | 未実施 | 実サービス上の予約保存・管理操作・LINE連携は未検証 |

以下はローカルで実施した修正と検証の記録。

| 不具合 | 修正内容 | 確認 |
|---|---|---|
| 不正クーポンによる0円予約 | クーポンは登録済みの自身のキーだけを許可。GAS送信前・受信時にも金額を検証 | 継承キー3種のAPI往復、非有限・null・負数・文字列金額の送信／保存拒否 |
| 同時書き込みで予約が消失 | 両GASの追記をSheets API `appendCells`へ変更。空き行番号を先取りせず、行追加と値保存を一括実行。復旧先の行が空または別予約なら新しい行へ復元 | 独立したGAS実行環境で4通りの割込み、API応答・カレンダー失敗時の復旧、予定増減、グリッド拡張、再送時の重複防止 |
| 復帰時にプランが戻る | 手動変更したプラン・日付をURLへ同期し、LINEの戻り先と下書きを一致。別プランのURLから開いた際は古い時間を破棄 | 日本語・英語・韓国語・繁体字で変更後の再読み込み |
| 空白付きクーポンの金額不一致 | 共通検証で空白を除去し、フォームにも正規化済みコードを保存 | プレビュー・予約API応答・GASペイロードの金額一致 |
| 別種ツアーの場所・担当を継承 | 種別が一致する予定だけから継承。新設種別の場所・担当は空欄 | S3→C1の海亀は空欄、ナイトは既存値を維持 |
| LINEプレビュー後の誤った競合 | 更新・送信・取消後に最新バージョンを返し、画面へ反映。取消処理中はダイアログを維持 | プレビュー→取消／送信→連続編集。送信後の再読込失敗でも送信成功を維持 |
| 日本語LINE本文の保存上限 | プレビューを2000コード単位ずつ分割保存。旧形式の読取、期限切れ・取消・失敗時の削除に対応 | 日本語4500文字、絵文字、旧形式、部分保存失敗、他予約のプレビュー保持 |
| 滞在・スクロールの記録先違い | 計測開始時のページパスを終了イベントへ明示 | `/blog`→`/book`のブラウザ内イベント捕捉、ページ別言語の単体検証 |
| 存在しない予約日 | UTC変換後の年月日が入力と一致するか検証 | 月末、閏日、2100年・2400年、不正月・不正日。拒否時の外部通信なし |

追加修正: `削除済み予約`シートの行・列を必要に応じて拡張し、旧スキーマへ紹介列の見出しも追加する。26列／51列／55列で満杯の退避先を検証した。予約APIの不正JSONオブジェクト・参加者にも400を返す。

## ローカル検証

- `npm test`: **211件成功、失敗0**。監査時183件から28件追加。受付の`Code.gs`昇格後にも同じ211件が成功（`/tmp/umigame-deploy-record-tests.log`）。
- `npm run lint`: 警告・エラーなし。
- `npx tsc --noEmit --incremental false`: 成功。
- `npm run build`: 成功、104ページ生成。
- 15プラン×4言語の予約API検証: 60通りすべて想定どおり（受付26・規則どおり拒否34）。
- 本番ビルドをlocalhostで起動し、幅390pxの4言語予約フォームを確認。プラン・日付・時間を変更して再読み込みしても選択を維持し、横はみ出し・ページ例外なし。別プランのURLを開いたときは古い時間を破棄。
- APIのLINE認証・GAS保存はモック、GASのシート・カレンダー・メール・LINEはメモリ内で検証。Google実サービス上の同時実行・実LINE認証往復は未検証。

検証ログ・ブラウザの捕捉結果は `/tmp/umigame-fix-*` に保存。日付入力のCLI `fill` はネイティブ日付欄を正しく操作できなかったため、ブラウザの標準value setterとinput/changeイベントで検証した。

## GASの反映対象

- 予約受付: `apps-script/umigame-reservation-admin/Code.gs`（`2026.09.08-1`）と同フォルダの`appsscript.json`。デプロイ案内時のファイル名は`Code.next.gs`で、完了報告後に内容を変えずに昇格済み。
- 管理画面: `apps-script/umigame-reservation-webapp/Code.gs`（`2026.09.08-1`）、`Index.html`、`Styles.html`、`App.html`、`appsscript.json`。紹介制度`2026.08.24-1`で追加されたHTMLも含めた一式を案内した。
- 受付側の旧`Code.gs`は`archive/Code.2026-09-08.before-audit-fixes.gs`へ保管済み。これは以前のリポジトリ基準コードで、以前の本番ソースとの一致は未確認。`Code.gs`と`Code.next.gs`を同じGASプロジェクトへ両方配置しない。
- 予約一覧の49列構成、既存の紹介制度、同一予約番号の再送判定を維持する。両GASのマニフェストには高度なGoogleサービスの **Google Sheets API v4（識別子 `Sheets`）** を追加済み。

`appendCells`はデータのある最終行の後へ追記し、必要な行も追加する。`batchUpdate`はリクエスト内の変更を一括適用する。この性質を使い、別GAS同士で空き行番号を競合させない。[AppendCellsRequest](https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets/request#AppendCellsRequest)、[batchUpdate](https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets/batchUpdate)。既存の空行チェックボックス除去はスキーマ修復時に行うが、残存値がある場合はその後ろへ安全に追記する。

## デプロイ後に残る作業・確認

両GASの新バージョンデプロイと、ローカルの正式ファイルへの昇格・本番状態の記録更新は完了。再度のファイル差し替えは不要。

1. 両GASでGoogle Sheets API v4と必要な権限が有効か確認する。標準Google Cloudプロジェクトを使う場合はCloud側でもSheets APIを有効にする。[Google公式の有効化手順](https://developers.google.com/apps-script/guides/services/advanced)。
2. 紹介制度の初期設定状況を確認する。未設定なら、既存手順に従って受付GASの`setupReferralProgram()`を一度実行し、紹介シートを確認する。`setupSheet()`は実行しない。
3. サイト側の修正をデプロイする。本セッションでは未実施のため、現在の本番反映状況を確認してから進める。
4. 許可されたテスト予約で料金、シート、カレンダー、プラン変更、変更履歴、LINEプレビューを確認する。実LINE送信は明示的に許可されたテスト先で行う。

## 別途残る確認事項

監査で追加確認事項とした依存パッケージの脆弱性更新、GASの公開・認証設定、受付への共有秘密検証、紹介成果シートの容量上限は今回の9件とは別に残る。詳細は[修正前の監査報告](full-site-audit-2026-09-08.md)を参照。

Vercel CLIの更新通知も継続している。互換性改善のため `npm i -g vercel@latest` での更新を強く推奨する。

# 変更履歴

## 2026-07-21

### レンタル希望の記録

- ウェットスーツ希望を参加者ごとに予約一覧K列へ記録
- 度付きマスク希望を参加者ごとに予約一覧K列へ記録
- 同じ参加者詳細を管理者メール・Googleカレンダー・確定LINEに反映
- 本番GASへ反映済み（ユーザー確認）

## 2026-07-16

### 現行コードの取り込み

- 現在の本番GAS 3,352行を`Code.gs`へ保存
- 共有された内容とSHA-256で完全一致を確認（最終改行のみ追加）
- `Code.gs`単体のJavaScript構文検査に合格
- 現行コードと安全送信変更を結合した`Code.next.gs`を作成

### 本番反映済み

- LINE送信をM・N・S列の即時送信から、T列チェックによる二段階確認へ変更
- U列へ送信予定・送信結果を表示
- `LINE送信履歴`シートへ送信日時・予約番号・本文・結果を記録
- `シートを初期化する`を通常メニューから非表示
- `setupSheet()`関数自体は緊急復旧用として残す
- ユーザーより本番GASへの貼り替え完了を確認
- 統合版`Code.next.gs`を正式な`Code.gs`へ昇格
- 反映前コードを`archive/Code.2026-07-16.before-line-safe.gs`へ退避

### シートへ反映済み

- `予約一覧`のT列に`LINE送信確認`を追加
- `予約一覧`のU列に`送信予定・結果`を追加
- `LINE送信履歴`シートを追加
- 変更直前バックアップを作成

### 検証

- JavaScript構文検査：合格
- 模擬環境で、M列・S列が即送信されないことを確認
- T列確認後に1回だけ送信されることを確認
- 成功履歴が1行追加されることを確認
- 予約一覧A〜S、M/Nプルダウン、既存180予約が不変であることを確認

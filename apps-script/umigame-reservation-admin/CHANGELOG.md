# 変更履歴

## 2026-09-08（`2026.09.08-1`、本番デプロイ済み・ユーザー報告）

- 受付・管理の両GASを新バージョンへデプロイしたとのユーザー報告を受領。その後、受付エディタのコード一致、Sheets API v4の設定、本番での不正金額の拒否を確認。[本番確認記録](../../docs/deployment-2026-09-08.md)。
- `Code.next.gs`の完全版を内容を変えずに`Code.gs`へ昇格。以前のリポジトリ基準コードを`archive/Code.2026-09-08.before-audit-fixes.gs`へ保管。
- シート・カレンダー・通知処理より前に、正の整数の予約金額と非負整数の割引額を検証。
- 予約追記をSheets APIの`appendCells`へ変更し、別プロジェクトの管理GASとの同時追記による上書きを防止。保存後も予約番号で再照合。
- 管理用入力規則を引き継ぎ、V列以降の旧チェックボックス入力規則を追記時に除去。
- 両GASのGoogle Sheets API有効化が必要。[検証と反映手順](../../docs/fixes-2026-09-08.md)。

## 2026-08-24

### 紹介パートナー / アフィリエイト制度（`2026.08.24-1`）

- 予約一覧の既存45列を動かさず、末尾46〜49列へ紹介情報を追加
- 紹介者マスタ、プラン別報酬ルール、予約番号一意の紹介成果シートを追加
- 売上率・予約固定・参加者単価、プラン個別優先、期間・有効判定を実装
- クーポン適用後の予約全体売上を使い、複数行セットでも成果は1件だけ作成
- 報酬条件と金額を受付時にスナップショット保存し、再送や率変更で上書きしない
- 紹介処理の例外を予約本体から分離
- 再実行安全な`setupReferralProgram()`で、かいた君50%を未登録時だけ追加

本番反映状況: **未反映。予約受付GAS再デプロイとsetup実行が必要**

2026-09-08追記: コードは`2026.09.08-1`のデプロイ対象に含む。両GASの新バージョンデプロイはユーザー報告済み。その後、管理画面で既存の紹介成果を確認できたため、初期設定の再実行は不要。

## 2026-08-20

### まるごと1日セットが海亀1行だけになる不具合

本番の`予約一覧`を調査し、V列（現在の`メールアドレス`列）全体に、旧
`LINE送信確認`列のチェックボックス入力規則が残っていることを確認した。
診断ログには、メールアドレスをV列へ書く際の入力規則違反が記録されていた。
このため45列の通常保存が失敗し、暫定の縮小書き込み経路でC5/C6の先頭にある
海亀行だけが残っていた。

対応:

- 本番シートV列の誤った入力規則を、セル値や書式を変えずに除去
- 影響を受けた9月21日の実予約を同じ予約番号のGoogleカレンダー3予定と照合し、
  海亀11:00（11,333円）・SUP12:30（11,333円）・ナイト19:20（11,334円）の
  3行、合計34,000円へ復元
- スキーマ版を`2026.08.20-1`へ更新し、V〜ASの不要な入力規則を自動除去
- 新規行の書き込み直前にもV〜ASの入力規則を除去し、挿入行が古い規則を
  継承しても予約保存を妨げないようにした
- 保存直後に予約番号・時間・プラン・金額を再読込して検証
- 同じ予約番号が一部の行しか持たない場合、重複成功扱いを禁止
- 入力規則残存・不完全な重複・正常な再送の回帰テストを追加

本番反映状況:

- 本番シートの入力規則修復と影響予約の復元: **完了**
- 恒久対策を含む`Code.gs`: **本番GASへの再デプロイ待ち**

## 2026-08-14

### プランの振り分けを`planId`優先へ修正（全体監査で発見）

備考欄（`specialRequests`）にはお客様の自由記述がそのまま入るのに、セットプランの
振り分けがその文字列を検索して決まっていた。そのため要望欄に書かれた言葉だけで
別プラン扱いになり、シートの行数・Googleカレンダーの予定・管理者メールの内訳が
実際の予約と食い違っていた。

実際に起きうる誤りと影響:

| 予約 | 要望欄の記述 | 修正前の扱い | 影響 |
|---|---|---|---|
| 昼夜セット C1/C2 | ドローンSUP | まるごと1日セット | 実在しないドローンSUPの行・カレンダー予定が作られる |
| 海空セット C3/C4 | ヤシガニ / ナイト | まるごと1日セット | ナイト開始時刻を取得できず`doPost`が失敗し、**予約自体がエラーになる** |
| 通常セット C1/C3/C5 | 貸切 | 貸切セット | 売上内訳が貸切単価で分割される（合計金額は変わらない） |
| 単品 S1〜S8 | `[COMBO booking]` | 海空セット | 単品予約が2行に分割される |

修正内容:

- サイトが常に送っている`planId`（S1〜S8 / C1〜C6）を最優先で使う
  `bookingPlanId_()` を追加し、`isSeaSkyComboBooking` / `isComboBooking` /
  `isPrivateComboBooking_` / `c5c6IsTripleBooking_` / `c5c6IsPrivateTriple_` が
  これを先に見るようにした
- `planId`が取れない古い送信・手動再送のときだけ、従来のテキスト判定へフォールバックする
- 金額・行の作り方・メール本文・カレンダー本文の組み立ては変更していない
- リポジトリ側に回帰テスト `lib/gas-plan-routing.test.mjs` を追加し、
  要望欄に上記の語を書いてもプラン振り分けが変わらないことを`npm test`で検査する

本番反映状況

- **反映済み（2026-08-14・ユーザー確認）**。`Code.gs`貼り替え＋新バージョンでのウェブアプリ再デプロイまで完了
- `setupSheet()`は実行していない

### `LineSafeSend.gs`を削除

- 内容が`Code.gs`末尾（LINE安全送信ブロック）と完全に同一だった
- 両方を同じGASプロジェクトへ貼ると`sendLineNotify`等のラップが二重に適用され、
  1回の送信で`LINE送信履歴`へ2行記録される事故になるため、重複ファイルを廃止
- 本番のGASプロジェクト側には元から`Code.gs`だけを貼る運用のため、動作への影響はない

### 予約再送の重複登録・大量メール防止（`2026.08.14-5`）

- 同じ入力内容の再送で同じ予約番号を使えるよう、サイト側の送信IDと連携
- GASのスクリプトロック内で予約番号を照合し、登録済みならシート・カレンダー・管理者メールを再実行しない
- 予約番号B列の最終データ行へ追記し、空行T列の`FALSE`によって1000行目へ飛ぶ問題を修正
- 空行T/Uの不要値だけを初回修復時に除去し、実予約行のLINE状態は保持
- `setupSheet()`によるT列全消去を廃止
- 管理者メールへお客様のメールアドレスを追加

本番反映状況

- **反映済み（2026-08-14・ユーザー確認）**
- 必ず予約受付GASを先に再デプロイし、その後サイトをデプロイする
- `setupSheet()`は実行していない

### 予約受信時のスキーマ確認を軽量化（`2026.08.14-4`）

- 通常の予約受信ごとにヘッダーとT/U列入力規則を書き直していた処理を廃止
- 初回修復・列追加・見出し不一致時だけ安全なスキーマ修復を実行
- U列へ誤って残った`TRUE`/`FALSE`だけを除去し、実際のLINE送信結果は保持
- 既存予約の内容・順序・顧客行動データは変更しない

本番反映状況

- **反映済み（2026-08-14・ユーザー確認）**
- `setupSheet()`は実行していない

### 列移行後の入力規則修正

- T/U列の挿入時に旧列から引き継がれた入力規則を解除
- U列を自由記述の送信結果列へ戻し、T列だけにチェックボックスを設定
- `セル U37 に入力したデータが入力規則に違反`する初回移行エラーを修正

### 予約一覧の列衝突修復と管理プランID

- 管理Webアプリと予約受付GASの列定義を45列へ統一
- LINE送信確認・結果をT/U、メール・Visitor IDをV/Wへ固定
- T/Uがメール・Visitor IDになっている衝突構成だけを検出し、既存行を削除せず2列挿入して値を分類・移行
- 新規予約で末尾の`管理プランID`へS1〜S8・C1〜C6を保存
- `setupSheet()`を使わず、通常の予約受信時に安全な列確認を実行

本番反映状況

- **反映済み（2026-08-14・ユーザー確認）**
- 先に反映済みの顧客・行動連携初版のデータを保持したまま移行する

## 2026-08-13

### 顧客情報と予約前行動履歴の連携

- 予約一覧の既存A〜S列を維持し、末尾へ顧客・行動分析用の列を追加
- メールアドレス、Visitor ID、Visit ID、予約ファネルID、同意バージョン・日時を保存
- 言語、着地ページ、参照元、UTM、予約送信ページ、デバイス・ブラウザ・OSを保存
- 参加者ごとの年齢、身長、体重、足サイズ、要望・アレルギー等を個別列へ保存
- 既存予約を削除せず、足りない列だけを末尾へ追加する`ensureBookingSchema_`を追加

本番反映状況

- **本番反映済み（2026-08-13・ユーザー確認）**。Apps Scriptの`Code.gs`貼り替え、保存、新バージョンでのウェブアプリ再デプロイまで完了
- `setupSheet()`は既存予約を消すため、この変更の反映には使用しない
## 2026-08-11

### ナイトツアー第3便の日付またぎ対応

- 単品ナイトツアーのGoogleカレンダー所要時間を、他の単品ツアーと共通の120分から、プラン表示と一致する90分へ修正
- 23:20開始の第3便が、予約日の23:20から翌日0:50までの予定として登録されることを確認
- セットプランは従来から90分・日付またぎ対応済みのため、処理変更なし

本番反映状況

- **反映済み（2026-08-16・ユーザー確認）**

## 2026-08-01

### 合計受取金額の表示

管理者メール・Googleカレンダーとも、金額の計算ロジックは変更していません。表示のみの変更です。

管理者メール

- 【予約内容】の`合計金額`を`合計受取金額`へ変更（単体プラン・全セット共通）
- クーポン利用時は`（通常料金 ¥X − クーポン ¥Y）`の内訳行を追加
- 海空セット・昼夜セット・まるごと1日セットの売上内訳の末尾へ`合計受取金額`を追加
- 内訳末尾の合計は`totalPrice`の再表示ではなく、内訳各行の実額を合算して算出
- 内訳の`/ クーポン-¥X`を`（クーポン適用済 -¥X）`へ変更し、二重減算の誤読を防止

Googleカレンダー

- 単体プランの`売上`へ`（クーポン適用後・当日この金額を受け取る）`を付記
- セットの各イベントへ【セット全体の受取金額】ブロックを追加（合計・担当ごとの内訳）
- セットの`売上`へ`（このツアー分の計上額）`を付記
- 現金の受取担当を「開始時刻が最も早いツアー」に決定し、該当イベントのみ`← このツアー開始時に全額受け取る`と表示
- 他のイベントは`（受取済み・このツアーでは受け取らない）`と表示し、二重受取を防止

検証

- `Code.gs`単体・`TestMail.gs`結合ともJavaScript構文検査に合格
- 模擬環境でメール6パターン（単体×クーポン有無／海空／昼夜／まるごと1日／端数）を確認
- 模擬環境でカレンダー4パターン計8イベントを確認し、内訳合計と受取額の一致、受取担当が各セット1イベントのみであることを確認
- `TestMail.gs`を追加（`testReceiveTotalEmail`で管理者メールの表示確認。シート・カレンダー・LINEには影響しない）

本番反映状況

- 管理者メール分：**反映済み（2026-08-01・ユーザー確認）**
- Googleカレンダー分：**反映済み（2026-08-13・ユーザー確認）**

補足：`doPost`はデプロイ済みバージョンのコードで動くため、エディタへ貼り付けて保存するだけでは反映されません。`デプロイ`→`デプロイを管理`→対象を編集→バージョンを`新バージョン`→`デプロイ`まで必要です（URLは変わりません）。

## 2026-07-21

### レンタル希望の記録

- ウェットスーツ希望を参加者ごとに予約一覧K列へ記録
- 度付きマスク希望を参加者ごとに予約一覧K列へ記録
- 同じ参加者詳細を管理者メール・Googleカレンダー・確定LINEに反映
- 本番GASへ反映済み（ユーザー確認）

## 2026-07-16

### 現行コードの取り込み

- 現在の本番GAS 3,352行を`Code.gs`へ保存
- 共有された内容とSHA-256で完全一致を確認（最終改行のみ追加）
- `Code.gs`単体のJavaScript構文検査に合格
- 現行コードと安全送信変更を結合した`Code.next.gs`を作成

### 本番反映済み

- LINE送信をM・N・S列の即時送信から、T列チェックによる二段階確認へ変更
- U列へ送信予定・送信結果を表示
- `LINE送信履歴`シートへ送信日時・予約番号・本文・結果を記録
- `シートを初期化する`を通常メニューから非表示
- `setupSheet()`関数自体は緊急復旧用として残す
- ユーザーより本番GASへの貼り替え完了を確認
- 統合版`Code.next.gs`を正式な`Code.gs`へ昇格
- 反映前コードを`archive/Code.2026-07-16.before-line-safe.gs`へ退避

### シートへ反映済み

- `予約一覧`のT列に`LINE送信確認`を追加
- `予約一覧`のU列に`送信予定・結果`を追加
- `LINE送信履歴`シートを追加
- 変更直前バックアップを作成

### 検証

- JavaScript構文検査：合格
- 模擬環境で、M列・S列が即送信されないことを確認
- T列確認後に1回だけ送信されることを確認
- 成功履歴が1行追加されることを確認
- 予約一覧A〜S、M/Nプルダウン、既存180予約が不変であることを確認

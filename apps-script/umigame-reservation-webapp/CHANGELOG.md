# 変更履歴

## 2026-09-08（`2026.09.08-1`、本番版21へ更新済み）

- プラン変更の追加行をSheets API `appendCells`で保存し、受付GASとの行競合を解消。復旧時は別予約の行を上書きしない。
- 新設ツアーへ異なる種別の開催場所・担当者を引き継がない。
- LINEプレビュー・送信・取消後の最新予約バージョンを画面へ反映。取消完了前の次の編集を防止し、送信後の再読込失敗は警告として扱う。
- LINEプレビューを分割保存し、日本語4500文字に対応。旧形式の読取、期限切れ、取消、部分失敗時の削除も対応。
- 行が増えるプラン変更と、削除済み予約の行・列容量不足を修正。旧退避シートへ紹介列の見出しも補完。
- ユーザーによるデプロイ後の公開版20で、App.html末尾323文字の貼り付け欠落を確認。検証済みファイルに一致するよう補完し、20:46 JSTに版21へ更新。
- コード・HTML一致、Sheetsサービス、予約一覧・紹介画面の正常起動を確認。実予約やLINE送信を伴う操作は未実施。[本番確認記録](../../docs/deployment-2026-09-08.md)。

## 2026-08-24

### 紹介成果管理（`2026.08.24-1`）

- 予約一覧の末尾4列を保持した49列スキーマへ更新
- 新規「紹介」メニューへ今月集計、紹介者別集計、直近成果を追加
- 紹介成果を自動開催済みにせず、未確定・確定・取消・支払済みとして手動管理
- 予約詳細へ紹介者、紹介コード、キャンペーン、取得日時を追加
- 満席への変更・重複予約削除時は未払い成果を取消へ連動し、支払済み成果は警告して保持
- 予約内容変更・削除退避・復旧でも紹介4列を失わないよう全行幅を49列へ統一

本番反映状況: **未反映。管理Webアプリの新バージョン再デプロイが必要**

2026-09-08追記: `Code.gs`・`Index.html`・`Styles.html`・`App.html`を`2026.09.08-1`のデプロイ対象に含む。両GASの新バージョンデプロイはユーザー報告済み。

## 2026-08-21

### 管理アプリの初期読み込みを軽量化（`2026.08.21-1`）

- 予約一覧のヘッダー・列構成確認を毎回から6時間ごとに変更し、通常起動のシート通信を削減
- 従来は2回読んでいたヘッダーを、定期確認時も1回で済むように変更
- 開催場所候補はシートの入力規則を6時間キャッシュし、通常起動時の読み取りを省略（保存時の実物確認は維持）
- 予約単位の顧客情報を各予定に重複して返さないようにし、初期HTMLのJSONサイズを削減
- Apps Scriptの実行ログへ`[ADMIN_LOAD]`として、認証・シート準備・予約読み取り・データ生成の所要時間を記録

本番反映状況

- **反映済み（2026-08-21・ユーザー実施）**。管理Webアプリの`Code.gs`と`Index.html`を貼り替え、新バージョンで再デプロイ済み

## 2026-08-16

### 単品ナイトツアーの既定所要時間を90分へ

ナイトツアーに23:20便を追加したのに合わせ、`adminGetDefaultDurationMinutes_` の
単品ナイトツアー（`本格ナイトツアー` / `【貸切】本格ナイトツアー`）を120分から
90分へ変更した。プラン詳細ページの表示「約1.5時間」と、予約受付GASの
`addToCalendar` が作る予定の長さに揃えるため。

- この既定値は「既存予定の長さを引き継げないとき」だけのフォールバック。
  通常の日時変更は変更前の予定の長さをそのまま使うため、既存予約への影響はない
- 23:20開始の予定は翌日0:50終了になる。`adminDateTimeFromTexts_` が日付を
  またいで正しく計算することを確認済み

本番反映状況

- **反映済み（2026-08-16・ユーザー確認）**。`2026.08.14-5` の再デプロイと同時に反映

## 2026-08-14

### 初期表示をHTML埋込方式へ変更（`2026.08.14-5`）

- GAS処理は完了しているのに`google.script.run`の初期応答だけが戻らず、ローダーが残る実環境事象を確認
- `doGet`で予約データを取得し、安全なJSONとしてHTMLへ直接埋め込む方式へ変更
- 初期表示と更新ボタンから大容量の`google.script.run`応答依存を除去
- 初期取得エラーもHTMLへ埋め込み、ローダーではなく具体的なエラー画面へ表示
- 顧客入力にHTML終了文字列が含まれてもスクリプトを壊さないエスケープを追加
- LINE送信後は予約一覧全件を再取得せず、送信結果と履歴だけを更新

本番反映状況

- **反映済み（2026-08-16・ユーザー確認）**
- 管理Webアプリの`Code.gs`・`Index.html`・`App.html`を貼り替える

### 削除確認・読み込み・カレンダー照合の安定化（`2026.08.14-4`）

- 予約削除時の予約番号再入力と二重確認を廃止し、対象内容を表示する1回の確認へ変更
- サーバー側はログイン権限・予約キー・更新版・確認フラグを引き続き検証
- 通信待ちを20秒から45秒へ延長し、手動更新失敗時は表示中の予約を消さないよう修正
- 応答データの形式検証と、予約詳細を素早く切り替えた際のLINE履歴競合防止を追加
- 読み込みのたびにT/U列の入力規則を書き直していた処理を初回修復時だけに限定
- U列へ残った`TRUE`/`FALSE`をLINE結果として表示せず、初回修復時に除去
- カレンダー予定を予約番号の完全一致で照合し、元日付の前後31日へ手動移動された予定も補助検索

本番反映状況

- **反映済み（2026-08-16・ユーザー確認）**
- `setupSheet()`は実行しない

### 読み込み画面の停止対策（`2026.08.14-3`）

- `DOMContentLoaded`後にApp.htmlが評価された場合でも確実に初期化
- 初期データをJSON文字列で返し、`google.script.run`のオブジェクト変換停止を回避
- GAS応答へ20秒のタイムアウトを追加
- JavaScript未起動・通信停止・描画エラーをローダー上へ具体的に表示
- エラー画面へ再読み込みボタンを追加し、無期限にローダーだけを表示しないよう修正

### 初回の列移行時に残る入力規則を修正（`2026.08.14-2`）

- T/U列の挿入時に旧列から引き継がれた入力規則を解除
- U列を自由記述の送信結果列へ戻し、T列だけへチェックボックスを再設定
- 既存の予約値は変更・削除せず、入力規則だけを修正

### 予約内容の後編集

- 予約詳細へ「予約内容を変更する」を追加
- 単品・2予定セット・3予定セットを相互に変更可能にした
- プラン、氏名、電話、メール、人数、料金、クーポン、参加者詳細・数値、要望、日時を編集可能にした
- 海空セット・まるごと1日セットのSUPを海亀開始90分後へ自動設定
- 予約一覧の行追加・再利用・余剰行クリアとGoogleカレンダー予定の再作成を一括処理
- 処理途中で失敗した場合に、予約一覧とカレンダーを変更前へ戻す処理を追加
- 変更理由を必須にし、`予約変更履歴`へ変更前後JSONと概要を記録
- 保存時はLINEを自動送信せず、確認用の変更案内文だけを作成
- 予約受付GASと管理Webアプリの列定義を45列へ統一し、`管理プランID`を追加
- T/U列がメール・Visitor IDと衝突している構成を、既存行を削除せず自動分離する移行処理を追加

本番反映状況

- **反映済み（2026-08-16・ユーザー確認）**
- `setupSheet()`は既存予約を消すため、この変更の反映には使用しない

## 2026-07-18

### 重複予約の安全削除

- 予約詳細の一番下に「この予約を削除する」を追加
- 削除前に確認画面・予約番号入力・最終確認を必須化
- 単品予約は1行、セット予約は同じ予約番号の2〜3行をまとめて削除
- 削除前の全データを`削除済み予約`シートへ退避
- 同じ予約番号のGoogleカレンダー予定もまとめて削除
- LINE通知は送信しない仕様
- カレンダーまたは予約一覧の削除失敗時に自動復旧する処理を追加
- 成功・予約番号不一致・カレンダー失敗・シート失敗の模擬テストに合格

### セット予約の個別日付変更

- 昼夜セットのウミガメとナイトを別の日付・時間へ変更可能にした
- 海空セットは海亀とSUPを同日のまま、SUPを海亀開始90分後へ自動設定
- まるごと1日セットは海亀・SUPを同日連続、ナイトだけ別日へ変更可能にした
- 予約一覧・Googleカレンダー・確定・満席LINEを個別日付に対応
- 今日・明日・期間絞り込みで、セット内のいずれかの予定日を判定
- 個別日付の更新・カレンダー同期・失敗時ロールバック模擬テストに合格

### 確定前の予約日時変更

- 未対応予約に「予約日時を変更する」を追加
- 予約一覧とGoogleカレンダーの日付・時間を同時更新
- 更新失敗時にシートとカレンダーを元の日時へ戻すロールバックを追加
- 自動復旧にも失敗した場合は、両方の手動確認が必要と明示
- 海空セット・まるごと1日セットのSUPを海亀開始の90分後へ自動設定
- 昼夜セットの海亀・ナイト時間は個別入力
- 日時変更前に作成されたLINE送信待ちを自動取消し
- カレンダー認可用の `adminAuthorizeCalendarAccess` を追加
- 通常予約・昼夜セット・海空セット・まるごと1日セットの日時計算とカレンダーロールバック模擬テスに合格

## 2026-07-17

### 実際の運用順序へ調整

- 詳細画面で予約ステータスの確定を最初の操作として明示
- 開催場所の要確認を「明日の昼ツアー」と「本日のナイトツアー」に限定
- セット予約は昼予定のいずれかに場所があれば昼の案内済みと判定
- 担当スタッフを任意項目として表示

### 本番前の安全性・スマホ表示改善

- LINE送信にUUIDリトライキーを付与し、通信再試行時の二重送信を防止
- LINE送信成功後は履歴やU列の記録に失敗しても再送されない順序へ変更
- ステータス通知の送信済み情報をスクリプトプロパティにも保存
- 履歴記録失敗をLINE送信結果と分離し、管理画面へ警告表示
- スマホの確定売上カードで長い金額が切れないよう調整

## 2026-07-16

### 管理Webアプリ初版

- 既存予約受付GASから独立した管理Webアプリ専用プロジェクトを新設
- 予約番号単位で予約一覧を集約
- 昼夜セット、海空セット、まるごと1日セットを1予約として表示
- 今日・明日・未対応・開催場所未設定・確定売上の集計を追加
- PC用一覧とスマホ用カードUIを追加
- 予約詳細、参加者詳細、各構成プランの編集UIを追加
- ステータス、開催場所、スタッフの更新処理を追加
- 更新前の予約バージョン確認による上書き防止を追加
- LINE全文プレビューと30分有効の送信確認トークンを追加
- LINE成功履歴を使った確定・満席通知の二重送信防止を追加
- `LINE送信履歴`への記録と画面表示を追加
- `管理アプリ操作履歴`への監査記録を追加
- ブラウザへLINE User IDを返さない公開データ変換を追加
- 初回の許可メール設定を確認する`adminWhoAmI`を追加
- JavaScript構文検査とセット予約・金額・LINE文面の模擬テストに合格
